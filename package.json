{
  "name": "start-to-finish-js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "jquery": "^3.5.1"
  },
  "devDependencies": {
    "webpack": "^5.17.0",
    "webpack-cli": "^4.4.0"
  },
  "scripts": {
    "watch": "export NODE_ENV=development && npx webpack --config webpack.config.js --mode=development --watch",
    "dev": "export NODE_ENV=development && npx webpack --config webpack.config.js --mode=development",
    "build": "export NODE_ENV=production && npx webpack --config webpack.config.js --mode=production"
  },
  "author": "Robbie Sherrard",
  "license": "ISC"
}
